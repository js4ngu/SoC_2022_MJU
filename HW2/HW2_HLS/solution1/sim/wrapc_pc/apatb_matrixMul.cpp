// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "lm"
#define AUTOTB_TVIN_lm  "../tv/cdatafile/c.matrixMul.autotvin_lm.dat"
// wrapc file define: "ln"
#define AUTOTB_TVIN_ln  "../tv/cdatafile/c.matrixMul.autotvin_ln.dat"
// wrapc file define: "lp"
#define AUTOTB_TVIN_lp  "../tv/cdatafile/c.matrixMul.autotvin_lp.dat"
// wrapc file define: "in_r"
#define AUTOTB_TVIN_in_r  "../tv/cdatafile/c.matrixMul.autotvin_in_r.dat"
// wrapc file define: "c"
#define AUTOTB_TVOUT_c  "../tv/cdatafile/c.matrixMul.autotvout_c.dat"
#define AUTOTB_TVIN_c  "../tv/cdatafile/c.matrixMul.autotvin_c.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "c"
#define AUTOTB_TVOUT_PC_c  "../tv/rtldatafile/rtl.matrixMul.autotvout_c.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			lm_depth = 0;
			ln_depth = 0;
			lp_depth = 0;
			in_r_depth = 0;
			c_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{lm " << lm_depth << "}\n";
			total_list << "{ln " << ln_depth << "}\n";
			total_list << "{lp " << lp_depth << "}\n";
			total_list << "{in_r " << in_r_depth << "}\n";
			total_list << "{c " << c_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int lm_depth;
		int ln_depth;
		int lp_depth;
		int in_r_depth;
		int c_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void matrixMul (
int lm,
int ln,
int lp,
unsigned char in[256][128],
unsigned int c[128][128]);

void AESL_WRAP_matrixMul (
int lm,
int ln,
int lp,
unsigned char in[256][128],
unsigned int c[128][128])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "c"
		aesl_fh.read(AUTOTB_TVOUT_PC_c, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_c, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_c, AESL_token); // data

			sc_bv<32> *c_pc_buffer = new sc_bv<32>[16384];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'c', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'c', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					c_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_c, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_c))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: c
				{
					// bitslice(31, 0)
					// {
						// celement: c(31, 0)
						// {
							sc_lv<32>* c_lv0_0_127_1_lv1_0_127_1 = new sc_lv<32>[16384];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: c(31, 0)
						{
							// carray: (0) => (127) @ (1)
							for (int i_0 = 0; i_0 <= 127; i_0 += 1)
							{
								// carray: (0) => (127) @ (1)
								for (int i_1 = 0; i_1 <= 127; i_1 += 1)
								{
									if (&(c[0][0]) != NULL) // check the null address if the c port is array or others
									{
										c_lv0_0_127_1_lv1_0_127_1[hls_map_index].range(31, 0) = sc_bv<32>(c_pc_buffer[hls_map_index].range(31, 0));
										hls_map_index++;
									}
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: c(31, 0)
						{
							// carray: (0) => (127) @ (1)
							for (int i_0 = 0; i_0 <= 127; i_0 += 1)
							{
								// carray: (0) => (127) @ (1)
								for (int i_1 = 0; i_1 <= 127; i_1 += 1)
								{
									// sub                    : i_0 i_1
									// ori_name               : c[i_0][i_1]
									// sub_1st_elem           : 0 0
									// ori_name_1st_elem      : c[0][0]
									// output_left_conversion : c[i_0][i_1]
									// output_type_conversion : (c_lv0_0_127_1_lv1_0_127_1[hls_map_index]).to_uint64()
									if (&(c[0][0]) != NULL) // check the null address if the c port is array or others
									{
										c[i_0][i_1] = (c_lv0_0_127_1_lv1_0_127_1[hls_map_index]).to_uint64();
										hls_map_index++;
									}
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] c_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "lm"
		char* tvin_lm = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_lm);

		// "ln"
		char* tvin_ln = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_ln);

		// "lp"
		char* tvin_lp = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_lp);

		// "in_r"
		char* tvin_in_r = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_in_r);

		// "c"
		char* tvin_c = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_c);
		char* tvout_c = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_c);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_lm, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_lm, tvin_lm);

		sc_bv<32> lm_tvin_wrapc_buffer;

		// RTL Name: lm
		{
			// bitslice(31, 0)
			{
				// celement: lm(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : lm
						// sub_1st_elem          : 
						// ori_name_1st_elem     : lm
						// regulate_c_name       : lm
						// input_type_conversion : lm
						if (&(lm) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> lm_tmp_mem;
							lm_tmp_mem = lm;
							lm_tvin_wrapc_buffer.range(31, 0) = lm_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_lm, "%s\n", (lm_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_lm, tvin_lm);
		}

		tcl_file.set_num(1, &tcl_file.lm_depth);
		sprintf(tvin_lm, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_lm, tvin_lm);

		// [[transaction]]
		sprintf(tvin_ln, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_ln, tvin_ln);

		sc_bv<32> ln_tvin_wrapc_buffer;

		// RTL Name: ln
		{
			// bitslice(31, 0)
			{
				// celement: ln(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : ln
						// sub_1st_elem          : 
						// ori_name_1st_elem     : ln
						// regulate_c_name       : ln
						// input_type_conversion : ln
						if (&(ln) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> ln_tmp_mem;
							ln_tmp_mem = ln;
							ln_tvin_wrapc_buffer.range(31, 0) = ln_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_ln, "%s\n", (ln_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_ln, tvin_ln);
		}

		tcl_file.set_num(1, &tcl_file.ln_depth);
		sprintf(tvin_ln, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_ln, tvin_ln);

		// [[transaction]]
		sprintf(tvin_lp, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_lp, tvin_lp);

		sc_bv<32> lp_tvin_wrapc_buffer;

		// RTL Name: lp
		{
			// bitslice(31, 0)
			{
				// celement: lp(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : lp
						// sub_1st_elem          : 
						// ori_name_1st_elem     : lp
						// regulate_c_name       : lp
						// input_type_conversion : lp
						if (&(lp) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> lp_tmp_mem;
							lp_tmp_mem = lp;
							lp_tvin_wrapc_buffer.range(31, 0) = lp_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_lp, "%s\n", (lp_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_lp, tvin_lp);
		}

		tcl_file.set_num(1, &tcl_file.lp_depth);
		sprintf(tvin_lp, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_lp, tvin_lp);

		// [[transaction]]
		sprintf(tvin_in_r, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_in_r, tvin_in_r);

		sc_bv<8>* in_r_tvin_wrapc_buffer = new sc_bv<8>[32768];

		// RTL Name: in_r
		{
			// bitslice(7, 0)
			{
				int hls_map_index = 0;
				// celement: in(7, 0)
				{
					// carray: (0) => (255) @ (1)
					for (int i_0 = 0; i_0 <= 255; i_0 += 1)
					{
						// carray: (0) => (127) @ (1)
						for (int i_1 = 0; i_1 <= 127; i_1 += 1)
						{
							// sub                   : i_0 i_1
							// ori_name              : in[i_0][i_1]
							// sub_1st_elem          : 0 0
							// ori_name_1st_elem     : in[0][0]
							// regulate_c_name       : in
							// input_type_conversion : in[i_0][i_1]
							if (&(in[0][0]) != NULL) // check the null address if the c port is array or others
							{
								sc_lv<8> in_tmp_mem;
								in_tmp_mem = in[i_0][i_1];
								in_r_tvin_wrapc_buffer[hls_map_index].range(7, 0) = in_tmp_mem.range(7, 0);
                                 		       hls_map_index++;
							}
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 32768; i++)
		{
			sprintf(tvin_in_r, "%s\n", (in_r_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_in_r, tvin_in_r);
		}

		tcl_file.set_num(32768, &tcl_file.in_r_depth);
		sprintf(tvin_in_r, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_in_r, tvin_in_r);

		// release memory allocation
		delete [] in_r_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_c, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_c, tvin_c);

		sc_bv<32>* c_tvin_wrapc_buffer = new sc_bv<32>[16384];

		// RTL Name: c
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: c(31, 0)
				{
					// carray: (0) => (127) @ (1)
					for (int i_0 = 0; i_0 <= 127; i_0 += 1)
					{
						// carray: (0) => (127) @ (1)
						for (int i_1 = 0; i_1 <= 127; i_1 += 1)
						{
							// sub                   : i_0 i_1
							// ori_name              : c[i_0][i_1]
							// sub_1st_elem          : 0 0
							// ori_name_1st_elem     : c[0][0]
							// regulate_c_name       : c
							// input_type_conversion : c[i_0][i_1]
							if (&(c[0][0]) != NULL) // check the null address if the c port is array or others
							{
								sc_lv<32> c_tmp_mem;
								c_tmp_mem = c[i_0][i_1];
								c_tvin_wrapc_buffer[hls_map_index].range(31, 0) = c_tmp_mem.range(31, 0);
                                 		       hls_map_index++;
							}
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 16384; i++)
		{
			sprintf(tvin_c, "%s\n", (c_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_c, tvin_c);
		}

		tcl_file.set_num(16384, &tcl_file.c_depth);
		sprintf(tvin_c, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_c, tvin_c);

		// release memory allocation
		delete [] c_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		matrixMul(lm, ln, lp, in, c);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_c, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_c, tvout_c);

		sc_bv<32>* c_tvout_wrapc_buffer = new sc_bv<32>[16384];

		// RTL Name: c
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: c(31, 0)
				{
					// carray: (0) => (127) @ (1)
					for (int i_0 = 0; i_0 <= 127; i_0 += 1)
					{
						// carray: (0) => (127) @ (1)
						for (int i_1 = 0; i_1 <= 127; i_1 += 1)
						{
							// sub                   : i_0 i_1
							// ori_name              : c[i_0][i_1]
							// sub_1st_elem          : 0 0
							// ori_name_1st_elem     : c[0][0]
							// regulate_c_name       : c
							// input_type_conversion : c[i_0][i_1]
							if (&(c[0][0]) != NULL) // check the null address if the c port is array or others
							{
								sc_lv<32> c_tmp_mem;
								c_tmp_mem = c[i_0][i_1];
								c_tvout_wrapc_buffer[hls_map_index].range(31, 0) = c_tmp_mem.range(31, 0);
                                 		       hls_map_index++;
							}
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 16384; i++)
		{
			sprintf(tvout_c, "%s\n", (c_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_c, tvout_c);
		}

		tcl_file.set_num(16384, &tcl_file.c_depth);
		sprintf(tvout_c, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_c, tvout_c);

		// release memory allocation
		delete [] c_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "lm"
		delete [] tvin_lm;
		// release memory allocation: "ln"
		delete [] tvin_ln;
		// release memory allocation: "lp"
		delete [] tvin_lp;
		// release memory allocation: "in_r"
		delete [] tvin_in_r;
		// release memory allocation: "c"
		delete [] tvout_c;
		delete [] tvin_c;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

